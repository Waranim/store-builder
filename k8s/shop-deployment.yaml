apiVersion: apps/v1
kind: Deployment
metadata: { name: shop , namespace: store-builder }
spec:
  replicas: 2
  selector: { matchLabels: { app: shop } }
  template:
    metadata: { labels: { app: shop } }
    spec:
      containers:
        - name: shop
          image: shop-service:1.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
          env:
            - name: POSTGRES_HOSTNAME
              value: "db"
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: store-builder-config
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: store-builder-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: store-builder-secrets
                  key: POSTGRES_PASSWORD
            - name: GATEWAY_URL
              value: "http://gateway:8080/shops"